재검증을 위해 14~18장을 순서대로 읽고, 지적된 7개 항목만 대조하겠습니다. 파일은 수정하지 않습니다.14~16장은 읽었습니다. 17·18장으로 이어가며 지적 항목을 대조하겠습니다.지적 7항목을 코드·서술·상호 참조와 대조하기 위해, 관련 구절을 다시 검색합니다.14장이 검색창을 전제로 쓰는지, 상호 참조가 실제로 깨졌는지 확인하겠습니다.필터 변경 때 목록이 같이 갱신되는지, 완성 앱 코드와 대조합니다.①~⑥은 해소됐다. 절 번호·그림 번호도 14.1~18.6, 그림 14.1~18.6까지 연속이고 본문 지칭과 맞다. **남은 문제는 ⑦ 상호 참조 3건**이다.

---

## ①~⑥ 재검증 (통과)

| 항목 | 위치 | 판정 |
|---|---|---|
| ① store 생성 + `filteredPlaces` → `store.all()` | 16.2 프롬프트·결과 코드 | 있음 |
| ② ‘추가 즉시 마커’ | 16.4 다리 설명 이후, 16.5·16.6 확인 | 전환/`notifyChange` 뒤에만 참이라고 못 박음 |
| ③ toast 생성 실습 | 15.4 프롬프트 + HTML/JS/CSS + 그림 15.6 | 있음. 16장은 15장 `toast`를 import만 함 |
| ④ `save()` ↔ 18장 `listeners` | 17.4 코드·정보 박스 ↔ 18장 도입 | ‘18장 예고편 / 텅 빈 명단 / 헛방송’이 양쪽에서 같음 |
| ⑤ 백업 훈련의 삭제 | 17.5 훈련 1~4 | 삭제를 19장으로 미루고, ‘실수’ 추가 후 가져오기로 되돌림 |
| ⑥ `stopPropagation` | 16.5 본문·요약·연습 2 | 정공법은 영역 분리, 한 줄은 보험. 코드와 모순 없음 |

---

## 남은 문제 (⑦ 상호 참조)

| 심각도 | 위치 | 내용 |
|---|---|---|
| **중요** | 18.1 (28행) | 필터 바 아래 빈 `<ul id="place-list">`가 **이미 있다**고 한다. 14~17장 어디에도 이 마크업을 만드는 프롬프트·결과 코드가 없다. 18.2는 `#place-list`에 그린다고만 한다. 요소가 없으면 `renderList()`가 `null.innerHTML`로 죽는다. |
| **중요** | 14.2 프롬프트 (52행), 확인 (87행), 그림 14.3 | 필터를 **「검색창 아래」**에 두라고 한다. 검색창 UI는 **15.2에서 처음** 만든다. 14장 시점에는 검색창이 없다. |
| **중요** | 14.4 (160행) ↔ 18.2 (106행) · 그림 18.3 | 14장은 18장에서 목록 갱신을 붙여도 **필터 클릭 코드는 한 줄도 안 바뀐다**고 한다. 18.2는 ☕ 필터를 누르면 **지도와 목록이 함께** 걸러진다고 확인시킨다. 그런데 18장은 필터 클릭에 `renderList()`를 넣는 코드를 보여 주지 않고, 18.5 구독은 **store 변경**만 다룬다. 필터는 store를 안 건드린다. 완성 앱(`app/src/sidebar.js` 27행)만 필터 클릭에서 `renderList()`를 호출한다. 책을 그대로 따라가면 18.2·그림 18.3의 동기화는 성립하지 않고, 14장의 약속도 깨진다. |

파일은 수정하지 않았다.
